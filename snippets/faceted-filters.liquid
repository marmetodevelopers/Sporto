{% comment %}
  @param filter_context [Required]
    The filter_context that these filters are drawing from. For example: collection or search.

  @param paginate [Required]
    The paginate object from pagination.

{% endcomment %}

<div
  data-component="faceted-filters"
  data-component-type="component"
  {% if tag != blank %}data-component-tag="{{ tag }}"
  {% endif %}class="faceted-filters"
  data-faceted-filters
>
  {% if section.settings.filters_show_filters or section.settings.filters_show_sorting or section.settings.filters_show_totals or filter_context == search %}
  <form
    class="faceted-filters__form"
    data-filter-form
  >
    {% if filter_context == search %}
      <div class="faceted-filters__search" data-filter-search-input>
        <div class="faceted-filters__search-field">
          <label class="visually-hidden" for="search-field">{{ 'search.search' | t }}</label>
          <input
            class="faceted-filters__search-input"
            id="search-field"
            type="search"
            name="q"
            value="{{ filter_context.terms | escape }}"
            placeholder="{{ 'search.search' | t }}"
          >
          {% comment %}Icon search:default begin{% endcomment %}
          <svg width="20" height="20" viewBox="0 0 19 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M2.25 8.25H1.5h.75Zm6-6V1.5v.75Zm7.941 16 .5.559 1.118-1-.5-.56-1.118 1.001ZM13.06 12.5a.75.75 0 1 0-1.118 1l1.118-1ZM1.5 8.25a6.75 6.75 0 0 0 1.977 4.773l1.06-1.06A5.25 5.25 0 0 1 3 8.25H1.5Zm1.977 4.773A6.75 6.75 0 0 0 8.25 15v-1.5a5.25 5.25 0 0 1-3.712-1.538l-1.061 1.061ZM8.25 15a6.75 6.75 0 0 0 4.773-1.977l-1.06-1.06A5.25 5.25 0 0 1 8.25 13.5V15Zm4.773-1.977A6.75 6.75 0 0 0 15 8.25h-1.5a5.25 5.25 0 0 1-1.538 3.712l1.061 1.061ZM15 8.25a6.75 6.75 0 0 0-1.977-4.773l-1.06 1.06A5.25 5.25 0 0 1 13.5 8.25H15Zm-1.977-4.773A6.75 6.75 0 0 0 8.25 1.5V3a5.25 5.25 0 0 1 3.712 1.538l1.061-1.061ZM8.25 1.5a6.75 6.75 0 0 0-4.773 1.977l1.06 1.06A5.25 5.25 0 0 1 8.25 3V1.5ZM3.477 3.477A6.75 6.75 0 0 0 1.5 8.25H3a5.25 5.25 0 0 1 1.538-3.712L3.477 3.477ZM11.941 13.5l4.25 4.75 1.118-1-4.25-4.75-1.118 1Z" fill="currentColor"/></svg>
          {% comment %}Icon search:default end{% endcomment %}
        </div>
      </div>
    {% endif %}

    {% if section.settings.filters_show_filters %}
      <div class="faceted-filters__filters" data-filter-disclosures>
        {%- for filter in filter_context.filters -%}
          {% case filter.type %}
            {% when 'boolean' or 'list' %}
              <details class="faceted-filter" data-filter-disclosure data-index="filter-{{ forloop.index }}">
                <summary class="faceted-filter__summary" data-filter-summary>
                  <span>
                    {{ filter.label | escape }}
                    {% if filter.active_values.size > 0 %}
                      <span class="faceted-filter__summary-count">({{ filter.active_values.size }})</span>
                    {% endif %}
                  </span>
                </summary>
                <div class="faceted-filter__content" data-filter-content tabindex="-1">
                  <div class="faceted-filter__header" data-filter-header>
                    <span class="faceted-filter__active-filters">{{ 'products.filters.filters_selected' | t: count: filter.active_values.size }}</span>

                    <a class="faceted-filter__remove" href="{{ filter.url_to_remove }}" >
                      {{ 'products.filters.reset' | t }}
                    </a>
                  </div>

                  <ul class="faceted-filter__list" role="list">
                    {%- for value in filter.values -%}
                      <li class="faceted-filter__list-item">
                        {% capture label_text %}
                          <span class="faceted-filter__text">{{ value.label | escape }}</span>
                          <span class="faceted-filter__count">{{ value.count }}</span>
                        {% endcapture %}

                        {% assign value_disabled = false %}
                        {% if value.count == 0 and value.active == false %}
                          {% assign value_disabled = true %}
                        {% endif %}

                        {% if filter.type == 'list' %}
                          {% case filter.presentation %}
                            {% when 'text' %}
                              {% comment %}Component input-checkbox-row:default begin{% endcomment %}
                              {% liquid
                                assign form_id_6af283f3 = form_id
                                assign form_id = 'desktop-filter'
                                assign field_name_6af283f3 = field_name
                                assign field_name = value.param_name
                                assign field_value_6af283f3 = field_value
                                assign field_value = value.value
                                assign checked_6af283f3 = checked
                                assign checked = value.active
                                assign disabled_6af283f3 = disabled
                                assign disabled = value_disabled
                              %}
                              {% comment %}
                                @param form_id [Optional]
                                  The id of the form that will help identify where this checkbox is.
                              
                                @param label_text [Required]
                                  The text for the label to be included.
                              
                                @param form_namespace [Required]
                                  The form's namespace that is used in "name" attribute as form_namespace[field_name].
                              
                                @param field_name [Required]
                                  The field name for the form. Will be used for the "name" attribute.
                              
                                @param field_value [Optional]
                                  The value for the checkbox.
                              
                                @param checked [Optional]
                                  Boolean. If true, the checkbox will appear checked.
                              
                                @param disabled [Optional]
                                  Boolean. If true, the checkbox will appear disabled.
                              
                                @param attributes [Optional]
                                  Additional attributes to append to the input field.
                              {% endcomment %}
                              
                              <div
                                data-component="input-checkbox-row"
                                data-component-type="component"
                                {% if tag != blank %}data-component-tag="{{ tag }}"
                                {% endif %}class="
                                  input-checkbox-row
                                  {% if checked %}input-checkbox-row--checked{% endif %}
                                  {% if disabled %}input-checkbox-row--disabled{% endif %}
                                "
                              >
                                <input
                                  class="input-checkbox-row__checkbox"
                                  id="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}"
                                  type="checkbox"
                                  name="{% if form_namespace %}{{ form_namespace }}[{{ field_name }}]{% else %}{{ field_name }}{% endif %}"
                                  {% if field_value %}value="{{ field_value }}"{% endif %}
                                  {% if checked %}checked{% endif %}
                                  {% if disabled %}disabled{% endif %}
                                  {{ attributes }}
                                >
                                <label class="input-checkbox-row__label" for="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}" tabindex="-1">
                                  {{ label_text }}
                                </label>
                              </div>
                              
                              {% liquid
                                assign form_id = form_id_6af283f3
                                assign form_id_6af283f3 = nil
                                assign field_name = field_name_6af283f3
                                assign field_name_6af283f3 = nil
                                assign field_value = field_value_6af283f3
                                assign field_value_6af283f3 = nil
                                assign checked = checked_6af283f3
                                assign checked_6af283f3 = nil
                                assign disabled = disabled_6af283f3
                                assign disabled_6af283f3 = nil
                              %}
                              {% comment %}Component input-checkbox-row:default end{% endcomment %}
                            {% else %}
                              {% comment %}Component input-checkbox-swatch-row:default begin{% endcomment %}
                              {% liquid
                                assign form_id_8dc61059 = form_id
                                assign form_id = 'desktop-filter'
                                assign field_name_8dc61059 = field_name
                                assign field_name = value.param_name
                                assign field_value_8dc61059 = field_value
                                assign field_value = value.value
                                assign checked_8dc61059 = checked
                                assign checked = value.active
                                assign disabled_8dc61059 = disabled
                                assign disabled = value_disabled
                                assign display_type_8dc61059 = display_type
                                assign display_type = value.display.type
                                assign display_value_8dc61059 = display_value
                                assign display_value = value.display.value
                              %}
                              {% comment %}
                                @param form_id [Optional]
                                  The id of the form that will help identify where this checkbox is.
                              
                                @param label_text [Required]
                                  The text for the label to be included.
                              
                                @param form_namespace [Required]
                                  The form's namespace that is used in "name" attribute as form_namespace[field_name].
                              
                                @param field_name [Required]
                                  The field name for the form. Will be used for the "name" attribute.
                              
                                @param field_value [Optional]
                                  The value for the checkbox.
                              
                                @param checked [Optional]
                                  Boolean. If true, the checkbox will appear checked.
                              
                                @param disabled [Optional]
                                  Boolean. If true, the checkbox will appear disabled.
                              
                                @param attributes [Optional]
                                  Additional attributes to append to the input field.
                              
                                @param display_type [Required]
                                  The type of visual representation, either "colors" or "image".
                              
                                @param display_value [Required]
                                  The visual representation, either a list of colors, or an image.
                              {% endcomment %}
                              
                              <div
                                data-component="input-checkbox-swatch-row"
                                data-component-type="component"
                                {% if tag != blank %}data-component-tag="{{ tag }}"
                                {% endif %}class="
                                  input-checkbox-swatch-row
                                  {% if checked %}input-checkbox-swatch-row--checked{% endif %}
                                  {% if disabled %}input-checkbox-swatch-row--disabled{% endif %}
                                "
                              >
                                <input
                                  class="input-checkbox-swatch-row__checkbox"
                                  id="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}"
                                  type="checkbox"
                                  name="{% if form_namespace %}{{ form_namespace }}[{{ field_name }}]{% else %}{{ field_name }}{% endif %}"
                                  {% if field_value %}value="{{ field_value }}"{% endif %}
                                  {% if checked %}checked{% endif %}
                                  {% if disabled %}disabled{% endif %}
                                  {{ attributes }}
                                >
                                <label class="input-checkbox-swatch-row__label" for="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}" tabindex="-1">
                                  {% comment %}Component faceted-filters-swatch:default begin{% endcomment %}
                                  {% comment %}
                                    @param display_type [Required]
                                      The type of visual representation, either "colors" or "image".
                                  
                                    @param display_value [Required]
                                      The visual representation, either a list of colors, or an image.
                                  {% endcomment %}
                                  
                                  <div
                                    data-component="faceted-filters-swatch"
                                    data-component-type="component"
                                    {% if tag != blank %}data-component-tag="{{ tag }}"
                                    {% endif %}class="
                                      faceted-filter-swatch
                                      faceted-filter-swatch--{{ settings.swatch_shape }}
                                    "
                                  >
                                    {%- case display_type -%}
                                      {%- when 'colors' -%}
                                        <div class="faceted-filter-swatch__color" style="--faceted-filter-swatch-color: {{ display_value }};"></div>
                                  
                                      {%- when 'image' -%}
                                        {{- display_value | image_url: width: 100 | image_tag: class: 'faceted-filter-swatch__image', alt: display_value.alt, loading: 'lazy' -}}
                                    {%- endcase -%}
                                  </div>
                                  
                                  {% comment %}Component faceted-filters-swatch:default end{% endcomment %}
                                  {{ label_text }}
                                </label>
                              </div>
                              
                              {% liquid
                                assign form_id = form_id_8dc61059
                                assign form_id_8dc61059 = nil
                                assign field_name = field_name_8dc61059
                                assign field_name_8dc61059 = nil
                                assign field_value = field_value_8dc61059
                                assign field_value_8dc61059 = nil
                                assign checked = checked_8dc61059
                                assign checked_8dc61059 = nil
                                assign disabled = disabled_8dc61059
                                assign disabled_8dc61059 = nil
                                assign display_type = display_type_8dc61059
                                assign display_type_8dc61059 = nil
                                assign display_value = display_value_8dc61059
                                assign display_value_8dc61059 = nil
                              %}
                              {% comment %}Component input-checkbox-swatch-row:default end{% endcomment %}
                          {% endcase %}
                        {% else %}
                          {% comment %}Component input-checkbox-row:default begin{% endcomment %}
                          {% liquid
                            assign form_id_d6373395 = form_id
                            assign form_id = 'desktop-filter'
                            assign field_name_d6373395 = field_name
                            assign field_name = value.param_name
                            assign field_value_d6373395 = field_value
                            assign field_value = value.value
                            assign checked_d6373395 = checked
                            assign checked = value.active
                            assign disabled_d6373395 = disabled
                            assign disabled = value_disabled
                          %}
                          {% comment %}
                            @param form_id [Optional]
                              The id of the form that will help identify where this checkbox is.
                          
                            @param label_text [Required]
                              The text for the label to be included.
                          
                            @param form_namespace [Required]
                              The form's namespace that is used in "name" attribute as form_namespace[field_name].
                          
                            @param field_name [Required]
                              The field name for the form. Will be used for the "name" attribute.
                          
                            @param field_value [Optional]
                              The value for the checkbox.
                          
                            @param checked [Optional]
                              Boolean. If true, the checkbox will appear checked.
                          
                            @param disabled [Optional]
                              Boolean. If true, the checkbox will appear disabled.
                          
                            @param attributes [Optional]
                              Additional attributes to append to the input field.
                          {% endcomment %}
                          
                          <div
                            data-component="input-checkbox-row"
                            data-component-type="component"
                            {% if tag != blank %}data-component-tag="{{ tag }}"
                            {% endif %}class="
                              input-checkbox-row
                              {% if checked %}input-checkbox-row--checked{% endif %}
                              {% if disabled %}input-checkbox-row--disabled{% endif %}
                            "
                          >
                            <input
                              class="input-checkbox-row__checkbox"
                              id="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}"
                              type="checkbox"
                              name="{% if form_namespace %}{{ form_namespace }}[{{ field_name }}]{% else %}{{ field_name }}{% endif %}"
                              {% if field_value %}value="{{ field_value }}"{% endif %}
                              {% if checked %}checked{% endif %}
                              {% if disabled %}disabled{% endif %}
                              {{ attributes }}
                            >
                            <label class="input-checkbox-row__label" for="{% if form_namespace %}{{ form_namespace }}-{% endif %}{{ field_name }}-{{ field_value }}{% if form_id %}-{{form_id}}{% endif %}" tabindex="-1">
                              {{ label_text }}
                            </label>
                          </div>
                          
                          {% liquid
                            assign form_id = form_id_d6373395
                            assign form_id_d6373395 = nil
                            assign field_name = field_name_d6373395
                            assign field_name_d6373395 = nil
                            assign field_value = field_value_d6373395
                            assign field_value_d6373395 = nil
                            assign checked = checked_d6373395
                            assign checked_d6373395 = nil
                            assign disabled = disabled_d6373395
                            assign disabled_d6373395 = nil
                          %}
                          {% comment %}Component input-checkbox-row:default end{% endcomment %}
                        {% endif %}
                      </li>
                    {%- endfor -%}
                  </ul>
                </div>
              </details>

            {% when 'price_range' %}
            <details class="faceted-filter" data-filter-disclosure data-index="{{ forloop.index }}">
              <summary class="faceted-filter__summary" data-filter-summary>
                <span>
                  {{ filter.label | escape }}
                  {% if filter.min_value.value or filter.max_value.value %}
                    <span class="faceted-filter__summary-count">({{ cart.currency.symbol }})</span>
                  {% endif %}
                </span>
              </summary>
              <div class="faceted-filter__content" data-filter-content>
                <div class="faceted-filter__header" data-filter-header>
                  {%- assign max_price_amount = filter.range_max | money | strip_html | escape -%}
                  <span class="faceted-filter__active-filters">{{ 'products.filters.max_price' | t: price: max_price_amount }}</span>

                  <a class="faceted-filter__remove" href="{{ filter.url_to_remove }}" >
                    {{ 'products.filters.reset' | t }}
                  </a>
                </div>

                <div class="faceted-filter__price-range">
                  {% assign label_text = 'products.filters.from' | t %}
                  {% comment %}Component input-currency:default begin{% endcomment %}
                  {% liquid
                    assign id_77bf1965 = id
                    assign id = 'Filter-Price-GTE'
                    assign field_name_77bf1965 = field_name
                    assign field_name = filter.min_value.param_name
                    assign field_value_77bf1965 = field_value
                    assign field_value = filter.min_value.value
                    assign placeholder_77bf1965 = placeholder
                    assign placeholder = '0'
                    assign max_value_77bf1965 = max_value
                    assign max_value = filter.range_max
                  %}
                  {% comment %}
                    @param id [Required]
                      A unique id to connect the label with the input.
                  
                    @param form_namespace [Required]
                      The form's namespace that is used in "name" attribute as form_namespace[field_name].
                  
                    @param label_text [Required]
                      The text for the label to be included.
                  
                    @param field_name [Required]
                      The field name for the form. Will be used for the "name" attribute.
                  
                    @param field_value [Required]
                      The value for the input.
                  
                    @param placeholder [Optional]
                      The placeholder to show in the field if no value is present.
                  
                    @param max_value [Required]
                      The maximum value that can be entered into the field. This will also populate the placeholder if no placeholder is provided.
                  
                    @param disabled [Optional]
                      Boolean. If true, the input will appear disabled.
                  
                    @param attributes [Optional]
                      Additional attributes to append to the input field.
                  {% endcomment %}
                  
                  {% liquid
                    assign currencies_using_comma_decimals = 'ANG,ARS,BRL,BYN,BYR,CLF,CLP,COP,CRC,CZK,DKK,EUR,HRK,HUF,IDR,ISK,MZN,NOK,PLN,RON,RUB,SEK,TRY,UYU,VES,VND' | split: ','
                    assign uses_comma_decimals = false
                    if currencies_using_comma_decimals contains cart.currency.iso_code
                      assign uses_comma_decimals = true
                    endif
                  %}
                  
                  <div data-component="input-currency" data-component-type="component" {% if tag != blank %}data-component-tag="{{ tag }}" {% endif %}class="input-currency">
                    <label
                      class="input-currency__label"
                      for="{{ id }}"
                    >
                      {{ label_text }}
                    </label>
                    <div class="input-currency__field">
                      <span class="input-currency__currency">{{ cart.currency.symbol }}</span>
                      <input
                        class="input-currency__input"
                        id="{{ id }}"
                        type="number"
                        name="{% if form_namespace %}{{ form_namespace }}[{{ field_name }}]{% else %}{{ field_name }}{% endif %}"
                        {%- if uses_comma_decimals -%}value="{{ field_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"{%- else -%}value="{{ field_value | money_without_currency | replace: ',', '' }}"{% endif %}
                        min="0"
                  
                        {%- if placeholder -%}
                          placeholder="{{ placeholder }}"
                        {%- elsif max_value -%}
                          {%- if uses_comma_decimals -%}
                            placeholder="{{ max_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                          {%- else -%}
                            placeholder="{{ max_value | money_without_currency | replace: ',', '' }}"
                          {% endif %}
                        {% endif %}
                  
                        {%- if max_value -%}
                          {%- if uses_comma_decimals -%}
                            max="{{ max_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                          {%- else -%}
                            max="{{ max_value | money_without_currency | replace: ',', '' }}"
                          {% endif %}
                        {%- endif -%}
                  
                        {{ attributes }}
                      >
                    </div>
                  </div>
                  
                  {% liquid
                    assign id = id_77bf1965
                    assign id_77bf1965 = nil
                    assign field_name = field_name_77bf1965
                    assign field_name_77bf1965 = nil
                    assign field_value = field_value_77bf1965
                    assign field_value_77bf1965 = nil
                    assign placeholder = placeholder_77bf1965
                    assign placeholder_77bf1965 = nil
                    assign max_value = max_value_77bf1965
                    assign max_value_77bf1965 = nil
                  %}
                  {% comment %}Component input-currency:default end{% endcomment %}

                  {% assign label_text = 'products.filters.to' | t %}
                  {% comment %}Component input-currency:default begin{% endcomment %}
                  {% liquid
                    assign id_77bf1965 = id
                    assign id = 'Filter-Price-LTE'
                    assign field_name_77bf1965 = field_name
                    assign field_name = filter.max_value.param_name
                    assign field_value_77bf1965 = field_value
                    assign field_value = filter.max_value.value
                    assign max_value_77bf1965 = max_value
                    assign max_value = filter.range_max
                  %}
                  {% comment %}
                    @param id [Required]
                      A unique id to connect the label with the input.
                  
                    @param form_namespace [Required]
                      The form's namespace that is used in "name" attribute as form_namespace[field_name].
                  
                    @param label_text [Required]
                      The text for the label to be included.
                  
                    @param field_name [Required]
                      The field name for the form. Will be used for the "name" attribute.
                  
                    @param field_value [Required]
                      The value for the input.
                  
                    @param placeholder [Optional]
                      The placeholder to show in the field if no value is present.
                  
                    @param max_value [Required]
                      The maximum value that can be entered into the field. This will also populate the placeholder if no placeholder is provided.
                  
                    @param disabled [Optional]
                      Boolean. If true, the input will appear disabled.
                  
                    @param attributes [Optional]
                      Additional attributes to append to the input field.
                  {% endcomment %}
                  
                  {% liquid
                    assign currencies_using_comma_decimals = 'ANG,ARS,BRL,BYN,BYR,CLF,CLP,COP,CRC,CZK,DKK,EUR,HRK,HUF,IDR,ISK,MZN,NOK,PLN,RON,RUB,SEK,TRY,UYU,VES,VND' | split: ','
                    assign uses_comma_decimals = false
                    if currencies_using_comma_decimals contains cart.currency.iso_code
                      assign uses_comma_decimals = true
                    endif
                  %}
                  
                  <div data-component="input-currency" data-component-type="component" {% if tag != blank %}data-component-tag="{{ tag }}" {% endif %}class="input-currency">
                    <label
                      class="input-currency__label"
                      for="{{ id }}"
                    >
                      {{ label_text }}
                    </label>
                    <div class="input-currency__field">
                      <span class="input-currency__currency">{{ cart.currency.symbol }}</span>
                      <input
                        class="input-currency__input"
                        id="{{ id }}"
                        type="number"
                        name="{% if form_namespace %}{{ form_namespace }}[{{ field_name }}]{% else %}{{ field_name }}{% endif %}"
                        {%- if uses_comma_decimals -%}value="{{ field_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"{%- else -%}value="{{ field_value | money_without_currency | replace: ',', '' }}"{% endif %}
                        min="0"
                  
                        {%- if placeholder -%}
                          placeholder="{{ placeholder }}"
                        {%- elsif max_value -%}
                          {%- if uses_comma_decimals -%}
                            placeholder="{{ max_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                          {%- else -%}
                            placeholder="{{ max_value | money_without_currency | replace: ',', '' }}"
                          {% endif %}
                        {% endif %}
                  
                        {%- if max_value -%}
                          {%- if uses_comma_decimals -%}
                            max="{{ max_value | money_without_currency | replace: '.', '' | replace: ',', '.' }}"
                          {%- else -%}
                            max="{{ max_value | money_without_currency | replace: ',', '' }}"
                          {% endif %}
                        {%- endif -%}
                  
                        {{ attributes }}
                      >
                    </div>
                  </div>
                  
                  {% liquid
                    assign id = id_77bf1965
                    assign id_77bf1965 = nil
                    assign field_name = field_name_77bf1965
                    assign field_name_77bf1965 = nil
                    assign field_value = field_value_77bf1965
                    assign field_value_77bf1965 = nil
                    assign max_value = max_value_77bf1965
                    assign max_value_77bf1965 = nil
                  %}
                  {% comment %}Component input-currency:default end{% endcomment %}
                </div>
              </div>
            </details>
          {% endcase %}
        {%- endfor-%}
        <noscript>
          {% assign submit_button_label = 'products.filters.submit' | t %}
          {% comment %}Component button:default begin{% endcomment %}
          {% liquid
            assign style_5dd2ec12 = style
            assign style = 'primary'
            assign size_5dd2ec12 = size
            assign size = 'small'
            assign text_5dd2ec12 = text
            assign text = submit_button_label
          %}
          {% comment %}
            @param style [Required]
              This is the style of the button: primary, secondary, tertiary, or text. Default primary.
          
            @param size [Required]
              This defines the size of the button: small, medium, or large. Default medium.
          
            @param text [Required]
              This is the text to be included in the button.
          
            @param attributes [Optional]
              This will include any additional attributes that the button needs. For example, aria-controls.
          {% endcomment %}
          
          <button
            data-component="button"
            data-component-type="component"
            {% if tag != blank %}data-component-tag="{{ tag }}"
            {% endif %}class="button button--{{ style }} button--{{ size }}{% if style == 'primary' or style == 'secondary' %} button--shadow{% endif %%}"
            {{ attributes }}
          >
            {{ text }}
          </button>
          
          {% liquid
            assign style = style_5dd2ec12
            assign style_5dd2ec12 = nil
            assign size = size_5dd2ec12
            assign size_5dd2ec12 = nil
            assign text = text_5dd2ec12
            assign text_5dd2ec12 = nil
          %}
          {% comment %}Component button:default end{% endcomment %}
        </noscript>
      </div>
    {% endif %}

    {% if section.settings.filters_show_sorting or section.settings.filters_show_totals %}
      <div class="faceted-filters__supplemental">
        {%- if section.settings.filters_show_sorting -%}
          <div class="faceted-filters__sort" data-filters-sort>
            {% assign label_text = 'products.filters.sort_by_label' | t %}
            {% assign button_label = 'products.filters.sort_button' | t %}
            {% assign selected_value = filter_context.sort_by | default: filter_context.default_sort_by %}

            {%- if filter_context == collection or search.results_count > 0 -%}
              <span class="faceted-filters__sort-by">{{ label_text }}:</span>
              {% comment %}Component disclosure:default begin{% endcomment %}
              {% liquid
                assign namespace_8d8d4b0c = namespace
                assign namespace = 'sort_by'
                assign heading_8d8d4b0c = heading
                assign heading = label_text
                assign value_list_8d8d4b0c = value_list
                assign value_list = filter_context.sort_options
                assign value_key_8d8d4b0c = value_key
                assign value_key = 'value'
                assign name_key_8d8d4b0c = name_key
                assign name_key = 'name'
                assign current_value_8d8d4b0c = current_value
                assign current_value = selected_value
                assign input_name_8d8d4b0c = input_name
                assign input_name = 'sort_by'
              %}
              {%- comment -%}
                @param namespace [Required]
                  The namespace that is used for aria-attributes.
              
                @param heading [Required]
                  The heading/text that is shown when opening the disclosure on mobile. Also used by screen readers to describe the contents of the disclosure.
              
                @param button_label [Optional]
                  Label for the no-js button.
              
                @param current_value [Required]
                  The currently selected value.
              
                @param value_list [Required]
                  The object that contains the list items to be rendered.
              
                @param value_key [Required]
                  The key that is used while looping over value_list to access the value of the item.
              
                @param name_key [Required - unless using is_country_selector]
                  The key that is used while looping over value_list to access the display name of the item.
              
                @param is_country_selector [Optional]
                  If true, this disclosure uses the fields for country localization (instead of making an entirely new component just for currency)
              
                @param input_name [Optional]
                  Used to specify the "name" attribute for the <input> element.
              
                @param fill_container [Optional]
                  If true, the disclocure toggle button will fill its parent container.
              
                @param always_show_heading [Optional]
                  If true, will always show the heading inside the button, instead of the selected value.
              {%- endcomment -%}
              
              {% liquid
                assign is_country_selector = is_country_selector | default: false
                assign always_show_heading = always_show_heading | default: false
              
                assign current_name = blank
                if is_country_selector
                  assign current_name = localization.country.name | append: ' (' | append: localization.country.currency.iso_code | append: ' ' | append: localization.country.currency.symbol | append: ')'
                else
                  for value_item in value_list
                    if value_item[value_key] == current_value
                      assign current_name = value_item[name_key]
                    endif
                  endfor
                endif
              %}
              
              <div data-component="disclosure" data-component-type="component" {% if tag != blank %}data-component-tag="{{ tag }}" {% endif %}class="disclosure{% if fill_container %} disclosure--fill{% endif %}">
                <script
                  type="application/json"
                  data-component-data
                >
                  {
                    "inputName": "{{ input_name }}",
                    "inputValue": "{{ current_value }}",
                    "alwaysShowHeading": {{ always_show_heading }}
                  }
                </script>
                <noscript>
                  <div class="disclosure__select-list">
                    <label class="visually-hidden" for="{{ namespace }}-select-{{ unique_id }}">
                      {{ heading | escape }}
                    </label>
                    <select
                      class="disclosure__select"
                      id="{{ namespace }}-select-{{ unique_id }}"
                      name="{{ input_name }}"
                    >
                      {% for value_item in value_list %}
                        <option
                          class="disclosure__option"
                          value="{{ value_item[value_key] }}"
                          {% if value_item[value_key] == current_value %}
                            selected
                          {% endif %}
                        >
                          {% if is_country_selector %}
                            {{ value_item.name }} ({{ value_item.currency.iso_code }} {{ value_item.currency.symbol }})
                          {% else %}
                            {{ value_item[name_key] }}
                          {% endif %}
                        </option>
                      {% endfor %}
                    </select>
                    {% comment %}Component button:default begin{% endcomment %}
                    {% liquid
                      assign style_0f9837c9 = style
                      assign style = 'primary'
                      assign size_0f9837c9 = size
                      assign size = 'small'
                      assign text_0f9837c9 = text
                      assign text = button_label
                    %}
                    {% comment %}
                      @param style [Required]
                        This is the style of the button: primary, secondary, tertiary, or text. Default primary.
                    
                      @param size [Required]
                        This defines the size of the button: small, medium, or large. Default medium.
                    
                      @param text [Required]
                        This is the text to be included in the button.
                    
                      @param attributes [Optional]
                        This will include any additional attributes that the button needs. For example, aria-controls.
                    {% endcomment %}
                    
                    <button
                      data-component="button"
                      data-component-type="component"
                      {% if tag != blank %}data-component-tag="{{ tag }}"
                      {% endif %}class="button button--{{ style }} button--{{ size }}{% if style == 'primary' or style == 'secondary' %} button--shadow{% endif %%}"
                      {{ attributes }}
                    >
                      {{ text }}
                    </button>
                    
                    {% liquid
                      assign style = style_0f9837c9
                      assign style_0f9837c9 = nil
                      assign size = size_0f9837c9
                      assign size_0f9837c9 = nil
                      assign text = text_0f9837c9
                      assign text_0f9837c9 = nil
                    %}
                    {% comment %}Component button:default end{% endcomment %}
                  </div>
                </noscript>
              
                <div class="disclosure__wrapper disclosure--no-js-hidden">
                  {% assign unique_id = "now" | date: "%N" | modulo: 1000 %}
                  <h2 class="visually-hidden" id="{{ namespace }}-heading-{{ unique_id }}">
                    {{ heading | escape }}
                  </h2>
                  <button
                    class="disclosure__toggle"
                    type="button"
                    aria-expanded="false"
                    aria-controls="{{ namespace }}-list-{{ unique_id }}"
                    aria-describedby="{{ namespace }}-heading-{{ unique_id }}"
                    data-disclosure-toggle
                  >
                    {% if always_show_heading %}
                      {{ heading }}
                    {% else %}
                      {{ current_name }}
                    {% endif %}
                  </button>
                  <div
                    class="disclosure__modal"
                    id="{{ namespace }}-list-{{ unique_id }}"
                    data-disclosure-modal
                  >
                    <h2 class="disclosure__heading">{{ heading }}</h2>
                    <button
                      class="disclosure__close"
                      data-disclosure-close
                    >
                      <span class="visually-hidden">{{ 'general.close' | t }}</span>
                    </button>
                    <ul class="disclosure__list">
                      {% for value_item in value_list %}
                        <li class="disclosure__list-item">
                          <a
                            class="
                              disclosure__link
                              {% if value_item[value_key] == current_value %}disclosure__link--selected{% endif %}
                            "
                            href="#"
                            data-value="{{ value_item[value_key] }}"
                            {% if value_item[value_key] == current_value %}
                              aria-current="true"
                            {% endif %}
                            data-disclosure-option
                          >
                            {% if is_country_selector %}
                              {{ value_item.name }} ({{ value_item.currency.iso_code }} {{ value_item.currency.symbol }})
                            {% else %}
                              {{ value_item[name_key] }}
                            {% endif %}
                          </a>
                        </li>
                      {% endfor %}
                    </ul>
                  </div>
                </div>
              </div>
              
              {% liquid
                assign namespace = namespace_8d8d4b0c
                assign namespace_8d8d4b0c = nil
                assign heading = heading_8d8d4b0c
                assign heading_8d8d4b0c = nil
                assign value_list = value_list_8d8d4b0c
                assign value_list_8d8d4b0c = nil
                assign value_key = value_key_8d8d4b0c
                assign value_key_8d8d4b0c = nil
                assign name_key = name_key_8d8d4b0c
                assign name_key_8d8d4b0c = nil
                assign current_value = current_value_8d8d4b0c
                assign current_value_8d8d4b0c = nil
                assign input_name = input_name_8d8d4b0c
                assign input_name_8d8d4b0c = nil
              %}
              {% comment %}Component disclosure:default end{% endcomment %}
            {%- endif -%}
          </div>
        {%- endif -%}

        {%- if section.settings.filters_show_totals -%}
          <div class="faceted-filters__result-count" role="status" data-filter-result-count>
            {%- if filter_context == search -%}
              {%- if search.results_count > 0 -%}
                {{ 'products.filters.product_count_simple' | t: count: search.results_count }}
              {%- endif -%}
            {%- else -%}
              {%- if section.settings.filters_show_filters -%}
                {%- if filter_context.products_count == filter_context.all_products_count -%}
                  {{ 'products.filters.product_count_simple' | t: count: filter_context.products_count }}
                {%- else -%}
                  {{ 'products.filters.product_count' | t: product_count: filter_context.products_count, count: filter_context.all_products_count }}
                {%- endif -%}
              {%- else -%}
                {% assign offset_max = paginate.current_offset | plus: paginate.page_size %}
                {% if offset_max > paginate.items %}
                  {% assign offset_max = paginate.items %}
                {% endif %}
                {% assign current_count = paginate.current_offset | plus: 1 | append: '-' | append: offset_max %}
                {{ 'products.filters.product_count' | t: product_count: current_count, count: paginate.items }}
              {%- endif -%}
            {%- endif -%}
          </div>
        {%- endif -%}
      </div>
    {% endif %}

    {% assign active_filter_count = 0 %}
    {%- for filter in filter_context.filters -%}
      {%- for value in filter.active_values -%}
        {% assign active_filter_count = active_filter_count | plus: 1 %}
      {%- endfor -%}
      {% if filter.type == "price_range" %}
        {%- if filter.min_value.value != nil or filter.max_value.value != nil -%}
          {% assign active_filter_count = active_filter_count | plus: 1 %}
        {% endif %}
      {% endif %}
    {%- endfor -%}

    <div
      class="
        faceted-filters__active
        {% if active_filter_count == 0 %}hidden{% endif %}
      "
      data-active-filters
      data-active-filters-count="{{ active_filter_count }}"
    >
      {%- for filter in filter_context.filters -%}
        {%- for value in filter.active_values -%}
          {% assign button_text = value.label | escape %}
          {% comment %}Component filters-tag-link:default begin{% endcomment %}
          {% liquid
            assign href_3659a02e = href
            assign href = value.url_to_remove
            assign text_3659a02e = text
            assign text = button_text
          %}
          {% comment %}
            @param href [Required]
              This is the anchor tag for the tag button.
          
            @param text [Required]
              This is the text to be included in the button.
          
            @param attributes [Optional]
              Any additional attributes to add to the button-link tag.
          {% endcomment %}
          
          <a
            data-component="filters-tag-link"
            data-component-type="component"
            {% if tag != blank %}data-component-tag="{{ tag }}"
            {% endif %}class="filters-tag-link"
            href="{{ href }}"
            {{ attributes }}
          >
            {{ text }}
          </a>
          
          {% liquid
            assign href = href_3659a02e
            assign href_3659a02e = nil
            assign text = text_3659a02e
            assign text_3659a02e = nil
          %}
          {% comment %}Component filters-tag-link:default end{% endcomment %}
        {%- endfor -%}
        {% if filter.type == "price_range" %}
          {%- if filter.min_value.value != nil or filter.max_value.value != nil -%}
            {% capture button_text %}
              {% if filter.min_value.value %}{{ filter.min_value.value | money }}{% else %}{{ 0 | money }}{% endif %} - {% if filter.max_value.value %}{{ filter.max_value.value | money }}{% else %}{{ filter.range_max | money }}{% endif %}
            {% endcapture %}
            {% comment %}Component filters-tag-link:default begin{% endcomment %}
            {% liquid
              assign href_3659a02e = href
              assign href = filter.url_to_remove
              assign text_3659a02e = text
              assign text = button_text
            %}
            {% comment %}
              @param href [Required]
                This is the anchor tag for the tag button.
            
              @param text [Required]
                This is the text to be included in the button.
            
              @param attributes [Optional]
                Any additional attributes to add to the button-link tag.
            {% endcomment %}
            
            <a
              data-component="filters-tag-link"
              data-component-type="component"
              {% if tag != blank %}data-component-tag="{{ tag }}"
              {% endif %}class="filters-tag-link"
              href="{{ href }}"
              {{ attributes }}
            >
              {{ text }}
            </a>
            
            {% liquid
              assign href = href_3659a02e
              assign href_3659a02e = nil
              assign text = text_3659a02e
              assign text_3659a02e = nil
            %}
            {% comment %}Component filters-tag-link:default end{% endcomment %}
          {%- endif -%}
        {% endif %}
      {%- endfor -%}
      <a class="faceted-filters__clear-all" href="{{ filter_context.url }}?sort_by={{ filter_context.sort_by }}{% if filter_context == search and filter_context.terms %}&q={{ filter_context.terms }}{% endif %}" >
        {{ 'products.filters.clear_all' | t }}
      </a>
    </div>
  </form>
  {% endif %}
</div>